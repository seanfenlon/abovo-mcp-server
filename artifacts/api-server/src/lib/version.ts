// Single source of truth for the server version reported to MCP clients and
// telemetry. Keep this in sync with artifacts/api-server/package.json and
// .mcp/server.json.
export const SERVER_VERSION = "1.1.1";
